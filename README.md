# mortise
A minimal, self-hosted AI gateway in Go. One OpenAI-compatible socket for vLLM fleets and external endpoints — routing, failover, rate limits, token accounting, and OTel tracing. No agent logic, no bloat.
